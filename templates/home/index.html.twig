{% extends 'base.html.twig' %}

{% block title %}Hello!{% endblock %}

{% block body %}
    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
        Bonjour
        {{app.user.name}}
    {% else %}
        Bonjour !
    {% endif %}
    
{% endblock %}
